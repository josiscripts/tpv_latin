import { Grid2X2, List, MoreHorizontal, Pencil, Plus, Search, SlidersHorizontal, Upload } from "lucide-react";
import { useState } from "react";
import { products } from "@/lib/pos-data";
import { PageHeader } from "./app-shell";
import { ProductImage, StockBadge, money } from "./pos-ui";
import { usePos } from "./pos-context";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogFooter, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger } from "@/components/ui/dropdown-menu";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";

export function ProductDialog({trigger}:{trigger?:React.ReactNode}){const {t}=usePos();return <Dialog><DialogTrigger asChild>{trigger??<Button><Plus/>{t("newProduct")}</Button>}</DialogTrigger><DialogContent className="max-h-[90dvh] overflow-y-auto sm:max-w-[760px]"><DialogHeader><DialogTitle>{t("newProduct")}</DialogTitle></DialogHeader><div className="grid grid-cols-2 gap-4 py-3"><div className="col-span-2 grid h-24 place-items-center rounded-md border border-dashed bg-muted/30"><Button variant="outline" size="sm"><Upload/>Subir imagen</Button></div>{[["Nombre","Ej. Inca Kola 300 ml"],["Código de barras","0000000000000"],["SKU","BEB-000-000"],["Costo proveedor","0,00 €"],["Precio venta","0,00 €"],["Stock inicial","0"],["Stock mínimo","0"]].map(x=><div key={x[0]} className="space-y-1.5"><Label>{x[0]}</Label><Input placeholder={x[1]}/></div>)}<div className="space-y-1.5"><Label>Categoría</Label><Select><SelectTrigger><SelectValue placeholder="Seleccionar"/></SelectTrigger><SelectContent><SelectItem value="bebidas">Bebidas</SelectItem><SelectItem value="snacks">Snacks</SelectItem></SelectContent></Select></div><div className="space-y-1.5"><Label>Proveedor</Label><Select><SelectTrigger><SelectValue placeholder="Seleccionar"/></SelectTrigger><SelectContent><SelectItem value="andes">Andes Foods</SelectItem><SelectItem value="caribe">Caribe Imports</SelectItem></SelectContent></Select></div><div className="space-y-1.5"><Label>IVA</Label><Select defaultValue="10"><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="4">4%</SelectItem><SelectItem value="10">10%</SelectItem><SelectItem value="21">21%</SelectItem></SelectContent></Select></div><div className="col-span-2 space-y-1.5"><Label>Descripción</Label><Textarea placeholder="Información adicional del producto"/></div></div><DialogFooter><Button variant="outline">{t("cancel")}</Button><Button>{t("save")}</Button></DialogFooter></DialogContent></Dialog>}
export function ProductsScreen(){const [grid,setGrid]=useState(false);const [q,setQ]=useState("");const {t}=usePos();const data=products.filter(p=>p.name.toLowerCase().includes(q.toLowerCase()));return <><PageHeader title={t("products")} subtitle="Catálogo de 248 productos" action={<ProductDialog/>}/><div className="mb-4 flex items-center gap-2"><div className="relative max-w-sm flex-1"><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={16}/><Input value={q} onChange={e=>setQ(e.target.value)} className="pl-9" placeholder="Buscar por nombre, código o SKU"/></div><Button variant="outline"><SlidersHorizontal/>Filtros</Button><div className="ml-auto flex rounded-md border bg-card p-1"><Button size="icon" variant={!grid?"secondary":"ghost"} className="size-8" onClick={()=>setGrid(false)}><List/></Button><Button size="icon" variant={grid?"secondary":"ghost"} className="size-8" onClick={()=>setGrid(true)}><Grid2X2/></Button></div></div>{grid?<div className="grid grid-cols-4 gap-4">{data.map(p=><div key={p.id} className="overflow-hidden rounded-lg border bg-card transition-shadow hover:shadow-md"><ProductImage product={p} size="lg"/><div className="p-4"><div className="flex items-start justify-between"><div><h3 className="text-sm font-semibold">{p.name}</h3><p className="mt-1 text-[11px] text-muted-foreground">{p.category} · {p.sku}</p></div><StockBadge state={p.state}/></div><div className="mt-4 flex items-end justify-between"><div><p className="text-lg font-bold">{money(p.price)}</p><p className="text-[11px] text-muted-foreground">Stock: {p.stock}</p></div><Button variant="outline" size="sm"><Pencil/>Editar</Button></div></div></div>)}</div>:<div className="overflow-hidden rounded-lg border bg-card"><div className="overflow-x-auto"><table className="w-full min-w-[1120px] text-left text-[11px]"><thead className="border-b bg-muted/50 text-[10px] uppercase text-muted-foreground"><tr>{["Producto","Código de barras","Categoría","Proveedor","P. venta","Costo","Beneficio","Stock","Estado",""] .map(x=><th key={x} className="px-4 py-3 font-semibold">{x}</th>)}</tr></thead><tbody>{data.map(p=><tr key={p.id} className="border-b transition-colors last:border-0 hover:bg-muted/35"><td className="px-4 py-2.5"><div className="flex items-center gap-3"><ProductImage product={p} size="sm"/><div><b>{p.name}</b><p className="text-[10px] text-muted-foreground">{p.sku}</p></div></div></td><td className="px-4 font-mono text-muted-foreground">{p.barcode}</td><td className="px-4">{p.category}</td><td className="px-4">{p.supplier}</td><td className="px-4 font-semibold">{money(p.price)}</td><td className="px-4 text-muted-foreground">{money(p.cost)}</td><td className="px-4 font-semibold text-success">{money(p.price-p.cost)}</td><td className="px-4 font-semibold">{p.stock}</td><td className="px-4"><StockBadge state={p.state}/></td><td className="px-3"><DropdownMenu><DropdownMenuTrigger asChild><Button size="icon" variant="ghost"><MoreHorizontal/></Button></DropdownMenuTrigger><DropdownMenuContent align="end"><DropdownMenuItem>Editar producto</DropdownMenuItem><DropdownMenuItem>Ver movimientos</DropdownMenuItem><DropdownMenuItem className="text-destructive">Eliminar</DropdownMenuItem></DropdownMenuContent></DropdownMenu></td></tr>)}</tbody></table></div><div className="flex items-center justify-between border-t px-4 py-3 text-[11px] text-muted-foreground"><span>Mostrando 1–8 de 248 productos</span><div className="flex gap-1"><Button variant="outline" size="sm" disabled>Anterior</Button><Button variant="outline" size="sm">Siguiente</Button></div></div></div>}</>}